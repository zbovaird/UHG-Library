# Install the library (editable, with dev tools):
#   pip install -e ".[dev]"
#   uv pip install -e ".[dev]"
#
# End users (PyPI):
#   pip install uhg
#   uv pip install uhg
#
# Optional extras: mcp, docs — see pyproject.toml [project.optional-dependencies].
